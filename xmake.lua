add_rules("mode.debug", "mode.release")
add_rules("plugin.compile_commands.autoupdate", {outputdir = ".vscode"})
set_languages("c++23")
set_project("ZeitKert")
set_version("0.1")
add_includedirs("src")
set_toolchains("clang")

add_requires("linenoise", "simdjson", "rapidjson", "gtest", "fmt 12.1.0", "spdlog v1.17.0")

target("libzeitkert")
    set_kind("static")
    add_files("src/**.cpp|main.cpp")
    add_packages("linenoise", "simdjson", "rapidjson", "fmt", "spdlog")

target("ZeitKert")
    set_kind("binary")
    add_files("src/main.cpp")
    add_deps("libzeitkert")
    add_packages("linenoise", "simdjson", "rapidjson", "fmt", "spdlog")

target("tests")
    set_kind("binary")
    add_cxxflags("-DTESTS")
    add_files("tests/**.cpp")
    add_deps("libzeitkert")
    add_syslinks("asan")
    add_cxxflags("-fsanitize=address", "-fno-omit-frame-pointer")
    add_packages("linenoise", "simdjson", "rapidjson", "gtest", "fmt", "spdlog")

target("bpm-bench")
    set_kind("binary")
    add_cxxflags("-DTESTS")
    add_files("benchmark/bpm-bench.cpp")
    add_deps("libzeitkert")
    add_packages("linenoise", "simdjson", "rapidjson", "fmt", "spdlog")

target("sqltest")
    set_kind("binary")
    add_files("tools/sqltest/SQLTest.cpp")
    add_deps("libzeitkert")
    add_packages("linenoise", "simdjson", "rapidjson", "fmt", "spdlog")

set_default("ZeitKert")